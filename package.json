{
  "name": "zadif-api-users",
  "version": "1.0.0",
  "main": "lambda.js",
  "license": "MIT",
  "scripts": {
    "watch": "tsc -w",
    "sls-offline": "sls offline --useChildProcesses",
    "start": "npm-run-all -p watch sls-offline",
    "install": "yarn install"
  },
  "husky": {
    "hooks": {
      "pre-push": "install"
    }
  },
  "dependencies": {
    "@apollo/federation": "^0.16.0",
    "apollo-server-lambda": "^2.13.1"
  },
  "devDependencies": {
    "aws-lambda": "^1.0.6",
    "npm-run-all": "^4.1.5",
    "serverless-domain-manager": "^4.0.1",
    "serverless-offline": "^6.1.5",
    "husky": "^4.2.5"
  }
}
